Place the provided hero image (the screenshot's right-side people image) here.

Filename recommended: `hero-image.png`

Path in this project (public):
`frontend/public/assets/hero-image.png`

The LandingPage component will load the image from `/assets/hero-image.png`.

If you prefer the file inside `src/assets`, you can update the import in
`frontend/src/pages/LandingPage.tsx` accordingly.
